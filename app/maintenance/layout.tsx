/**
 * Maintenance Page Layout
 * Simple layout without header/footer for the maintenance page
 */

export default function MaintenanceLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
